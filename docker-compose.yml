version: '3.8'
services:
  smallqr:
    build: .
    ports:
      - "8002:8002"
    environment:
      - FLASK_ENV=production
      - PORT=8002
    restart: unless-stopped
    working_dir: /app
    command: flask run --host=0.0.0.0 --port=8002
