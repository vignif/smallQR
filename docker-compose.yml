version: '3.8'

services:
  smallqr:
    build: .
    ports:
      - "8002:8002"
    environment:
      - FLASK_ENV=production
      - PORT=8002
      - BASE_PATH=/smallqr
    restart: unless-stopped
    working_dir: /app
